const express = require('express');
const router = express.Router();

// Placeholder — Student 3 will build these out
router.get('/', (req, res) => {
  res.json({ message: 'Booking routes coming soon' });
});

module.exports = router;
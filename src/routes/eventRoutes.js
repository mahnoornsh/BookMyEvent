const express = require('express');
const router = express.Router();

// Placeholder — Student 2 will build these out
router.get('/', (req, res) => {
  res.json({ message: 'Event routes coming soon' });
});

module.exports = router;